Что осталось исправитьПриоритет 1: Критическая функциональность1.Реализация случайной задержки в SiteCrawler (Критично для "вежливого" обхода):•Проблема: ТЗ требует задержку 0.5-5 секунд между запросами. Не реализовано.•Действие: Добавить случайную задержку (Thread.sleep()) между 500 мс и 5000 мс в SiteCrawler перед выполнением последующих HTTP-запросов.•Файлы: SiteCrawler.java.2.Обновление Site.statusTime при обработке каждой страницы (Критично для отслеживания статуса):•Проблема: ТЗ требует обновления status_time "при добавлении каждой страницы". В текущем коде оно обновляется только при изменении статуса сайта.•Действие: Изменить SiteCrawler или IndexingServiceImpl для обновления Site.statusTime (на текущее время) после каждой успешной обработки страницы.•Файлы: SiteCrawler.java, IndexingServiceImpl.java.Приоритет 2: Соответствие ТЗ и надежность3.Обеспечение полной очистки данных при переиндексации (Требование ТЗ):•Проблема: Необходимо убедиться, что при переиндексации сайта корректно очищаются все связанные данные из таблиц lemma и index.•Действие: Проверить логику в IndexingServiceImpl, чтобы убедиться, что данные lemma и index удаляются для сайта перед его повторной индексацией.•Файлы: IndexingServiceImpl.java.4.Изменение типа возвращаемого значения PageRepository.countBySiteId на long (Надежность):•Проблема: int может переполниться для очень большого количества страниц.•Действие: Изменить тип возвращаемого значения метода countBySiteId в PageRepository на long.•Файлы: PageRepository.java.5.Обеспечение запуска полной переиндексации SiteInitializationService (Требование ТЗ):•Проблема: ТЗ подразумевает полную переиндексацию при запуске приложения.•Действие: Убедиться, что SiteInitializationService инициирует полный процесс обхода и переиндексации для сконфигурированных сайтов при старте приложения.•Файлы: SiteInitializationService.java, IndexingServiceImpl.java.Приоритет 3: Мелкие детали ТЗ и верификация6.Выравнивание текста сообщения об ошибке "Индексация остановлена":•Действие: Скорректировать строку сообщения об ошибке в IndexingServiceImpl для точного соответствия ТЗ.•Файлы: IndexingServiceImpl.java.7.Верификация конвертации statusTime в DTO:•Действие: Проверить, что StatisticsServiceImpl корректно конвертирует LocalDateTime из Site в long Unix timestamp при создании SiteStatisticsDTO.•Файлы: StatisticsServiceImpl.java, DTO классы.8.Верификация функциональности поиска (сортировка, пагинация):•Действие: Проверить SearchServiceImpl, чтобы убедиться, что результаты поиска корректно сортируются по релевантности и что параметры offset/limit обрабатываются для пагинации.•Файлы: SearchServiceImpl.java.